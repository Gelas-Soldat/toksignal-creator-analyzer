# Current static version does not require environment variables.
# Future TikTok OAuth or payment integrations should keep secrets on the backend only.

# Example future variables, do not expose these in browser code:
# TIKTOK_CLIENT_KEY=
# TIKTOK_CLIENT_SECRET=
# STRIPE_SECRET_KEY=
